<resources>

    <!-- Dark mode app theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorTextDefault</item>
        <item name="android:fontFamily">@font/roboto_family</item>
    </style>

    <!--
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopUpPergunta">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>

    -->

    <style name="MyEditTextStyleArea" parent="Widget.AppCompat.EditText">
        <item name="colorControlActivated">@color/colorTextDefault</item>
        <item name="colorControlNormal">@color/colorTextDefault</item>
        <item name="backgroundTint">@color/colorTextDefault</item>
        <item name="android:textColor">@color/colorTextDefault</item>
    </style>


</resources>
